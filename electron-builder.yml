appId: com.anylink.llm-usage-widget
productName: LLM Usage Widget
# electron-updater 从 GitHub Releases 拉 latest.yml;GH_TOKEN 由 CI 提供
publish:
  provider: github
  owner: anylink
  repo: llm-usage-widget
directories:
  output: dist
  buildResources: build
files:
  - out/**
  - package.json
extraResources:
  - from: resources/vendors
    to: vendors
    filter: ["**/*"]
  - from: resources/logos
    to: logos
    filter: ["**/*"]
  - from: resources/icons
    to: icons
    filter: ["**/*"]
win:
  target:
    - nsis
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: false
  artifactName: ${productName}-${version}-${arch}-setup.${ext}
mac:
  target:
    - dmg
  category: public.app-category.utilities
  artifactName: ${productName}-${version}-${arch}.${ext}
linux:
  target:
    - AppImage
    - deb
  category: Utility
  artifactName: ${productName}-${version}-${arch}.${ext}
